{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "cl.exe build active file",
            "name": "Debug built exe (no rebuild)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\bin\\sandbox.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "environment": [],
            "console": "integratedTerminal",
        }
    ]
}
